import * as React from "react";

export default class Word extends React.Component<any, any> {
    render () {
        return (
            <div className="container-fluid word">
            </div>
        );
    }
}
