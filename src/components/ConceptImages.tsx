import * as React from "react";

export default class ConceptImages extends React.Component<any, any> {
    render () {
        return (
            <div className="container-fluid conceptImages">
            </div>
        );
    }
}
